//Unused currently, might come back to this in the future.

import React from 'react';
/*import Select, { GroupBase, Props} from 'react-select';

export function SearchSelect<
  Option,
  IsMulti extends boolean = false,
  Group extends GroupBase<Option> = GroupBase<Option>
>(props: Props<Option, IsMulti, Group>) {
  return (
    <Select 
	{...props} 
	
	styles={{
		container: (provided) => ({
			...provided,
			minWidth: '250px',
			width: '250px',
			padding: "0",
			margin: "0"
		}),
		control: (provided) => ({
			...provided,
			color: "#000000",
			padding: "0",
			margin: "0"
		}),
		option: (provided) => ({
			...provided,
			color: "#000000"
		}),
		valueContainer: (provided) => ({
			...provided,
			padding: "0 8px"
		}),
		input: (provided) => ({
			...provided,
			margin: "0",
			padding: "0"
		})
	}}

	theme={(theme) => ({
		...theme,
		colors: {
		  ...theme.colors,
		  primary: 'var(--select-primary)',
		  primary75: 'var(--select-primary75)',
		  primary50: 'var(--select-primary50)',
		  primary25: 'var(--select-primary25)',
		},
	})} 
		/>
  )
}*/